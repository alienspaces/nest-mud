/*
* This file was generated by a tool.
* Rerun `yarn generate:db` to regenerate this file.
*/

/* istanbul ignore file */

import { Repository } from './repository';
import { Table, Columns } from './metadata';
import { RowType, PrimaryKeyType } from './types';

const tables = {
{{#each grouped as |group key|}}
{{#each tables as |table|}}
    {{name}}: {
        {{#each columns}}
        {{propertyName}}: { type: '{{propertyType}}' as const{{#if nullable}}, isNullable: true{{/if}}{{#if isPrimaryKey}}, isPrimaryKey: true{{/if}} },
        {{/each}}
    },
{{/each}}
{{/each}}
} as const;

{{#each grouped as |group key|}}
{{#each tables as |table|}}

export type {{interfaceName}}Row = RowType<typeof tables.{{name}}>;
export type {{interfaceName}}PrimaryKey = PrimaryKeyType<typeof tables.{{name}}>;

@Table('{{name}}')
@Columns(tables.{{name}})
export class {{interfaceName}}Repository extends Repository<
    {{interfaceName}}Row,
    {{interfaceName}}PrimaryKey
> {};

{{/each}}
{{/each}}
