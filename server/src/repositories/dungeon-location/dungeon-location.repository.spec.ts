/*
 * This file was generated by a tool.
 * Rerun ./script/db-repositories.mjs to regenerate this file.
 */
import { Test, TestingModule } from '@nestjs/testing';
import { ConfigModule } from '@nestjs/config';

// Application
import { DatabaseModule, LoggerModule } from '@/core';
import { DungeonLocationRepositoryRecord, DungeonLocationRepository } from './dungeon-location.repository';

describe('DungeonLocationRepository', () => {
    let repository: DungeonLocationRepository;

    beforeEach(async () => {
        const module: TestingModule = await Test.createTestingModule({
            imports: [
                ConfigModule.forRoot({ ignoreEnvFile: false }),
                DatabaseModule,
                LoggerModule,
            ],
            providers: [DungeonLocationRepository],
        }).compile();

        repository = await module.resolve<DungeonLocationRepository>(
            DungeonLocationRepository,
        );
    });

    it('should be defined', () => {
        expect(repository).toBeDefined();
    });

    describe('buildInserSQL', () => {
        it('should return SQL', () => {
            const sql = repository.buildInsertSQL();
            expect(sql).toBeTruthy();
        });
    });
});
